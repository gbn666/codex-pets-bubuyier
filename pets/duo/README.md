# Duo Pet

The paired 一二 + 布布 pet is planned but not generated yet.

This folder intentionally does not include `pet.json` or `spritesheet.webp` until a real duo spritesheet has been created and validated.
